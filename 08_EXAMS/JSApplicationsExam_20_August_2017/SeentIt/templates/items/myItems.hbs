<section id="viewMyPosts">
    <div class="post post-content">
        <h1>Your Posts</h1>
    </div>
    <div class="posts">
        {{# each myPosts}}
            {{> singlePost}}
        {{else}}
            {{> noPosts}}
        {{/each}}
    </div>
</section>